import EmailFieldComponent from '@/components/field-types/EmailFieldComponent'

describe('EmailFieldComponent unit tests', () => {
  it('should load the component with "EmailFieldComponent" as a name', () => {
    expect(EmailFieldComponent.name).to.equal('EmailFieldComponent')
  })
})
